spring:
  application:
    name: eureka-server
  config:
    #import: "optional:configserver:http://localhost:7000"
    import: "optional:configserver:http://config-server:7000"
  cloud:
    config:
      enabled: true
      label: master
      uri: http://config-server:7000
